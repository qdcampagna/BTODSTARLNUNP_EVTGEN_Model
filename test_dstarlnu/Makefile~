TOP=/Users/qcampagna/EvtGen_Install
LIB=$(TOP)/lib
INC=$(TOP)/include

all: run_dstarlnu

run_dstarlnu: dstarlnu_test.cc
	g++ -I $(INC) `root-config --cflags --libs` -o $@ $? -L $(LIB) -lEvtGen -Wl, -rpath=$(LIB)
